<block wx:if="{{$taroCompReady}}">
    <view class="{{anonymousState__temp}}" style="{{anonymousState__temp2}}">
        <block wx:if="{{isShowDay}}">
            <at-countdown-item compid="{{$compid__782}}"></at-countdown-item>
        </block>
        <block wx:if="{{isShowHour}}">
            <at-countdown-item compid="{{$compid__783}}"></at-countdown-item>
        </block>
        <at-countdown-item compid="{{$compid__784}}"></at-countdown-item>
        <at-countdown-item compid="{{$compid__785}}"></at-countdown-item>
    </view>
</block>